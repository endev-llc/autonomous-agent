# OpenAI API Key - Replace with your actual API key
OPENAI_API_KEY=your-api-key-here

# Optional: Override the default agent goal
# AGENT_GOAL="Find, summarize, categorize, and score the latest physics articles from reputable sources"

# Optional: Run modes
# RUN_ONCE=true    # Run the agent once and exit
# WEB_ONLY=true    # Run only the web server (no agent)
# DEBUG=true       # Enable debug mode
